<div class="container mt-5">
<div class="row">
    <div class="col-xs-12 mt-5">
        <div class="title mt-5">
                    <h2>Step:2 気になる式場を選んでね</h2>
        </div>
        <% if flash[:notice] %>
        <div class="error-comment"><h2><%= flash[:notice] %></h2></div>
        <% end %>
    </div>
</div>

 <%# 共通ボディー開始タグここまで%>
<%# クライアント一覧 %>

<%= render 'client_plan_index', client_plans: @client_plans %>
 
<%# 共通ボディー閉じタグ %>
</div>